[project]
name = "omni-aria-agent"
version = "0.1.0"
description = "Aria — Omni Hotels & Resorts virtual guest services voice agent (LiveKit)"
requires-python = ">=3.10"
dependencies = [
    "livekit-agents[openai,deepgram,elevenlabs,silero]>=1.2.0",
    "python-dotenv>=1.0.0",
    "aiohttp>=3.9.0",
]

[tool.uv]
dev-dependencies = [
    "ruff>=0.5.0",
]
